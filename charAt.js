/* The charAt() method returns the character at a specified index
(position) in a string. 
*/
const name = "Akhil Hamal";
const text = "Messi";

console.log(`${name}`);
console.log(`First character of "${text}": "${text.charAt(0)}"`);